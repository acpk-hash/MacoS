# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/

# Environment files
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Tauri
src-tauri/target/
src-tauri/gen/

# TypeScript incremental build info and tsc -b emit artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts
# Full codex vendor tree staged by scripts/prepare-engine-codex.ps1
# (bin/codex.exe + codex-resources/ helpers + codex-path/rg.exe)
engine-codex/
# Self-contained pi runtime staged by scripts/prepare-engine-pi.ps1
# (node.exe + pi/dist + pi/node_modules)
engine-pi/

# Rust build output (root-level target/ if present)
target/

# Companion PWA build artifacts
companion/node_modules/
companion/dist/

# Windows executables
*.exe

# Windows null device artifact (never commit)
NUL
